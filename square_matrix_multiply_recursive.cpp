#include <bits/stdc++.h>

using namespace std;

vector<vector<int>> squareMatrixMultiplyRecursive(vector<vector<int>> &A, vector<vector<int>> &B) {
    int n = A.size();
    if(n == 0) {
        C
    }
}

int main() {
    return 0;
}